{
  "name": "omp-firecrawl",
  "version": "0.1.0",
  "private": true,
  "description": "Firecrawl v2 API as omp/pi tools: search, developer index, research index, scrape, crawl, map, batch, parse, extract, agent, interact, monitor, account.",
  "type": "module",
  "license": "MIT",
  "omp": {
    "version": "0.1.0",
    "description": "Firecrawl-backed web search, scraping, crawling and research for omp",
    "extensions": ["./src/index.ts"]
  },
  "pi": {
    "version": "0.1.0",
    "description": "Firecrawl-backed web search, scraping, crawling and research for pi",
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "format": "biome format --write src scripts tests",
    "check": "tsc --noEmit && biome check src scripts tests && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.14",
    "@oh-my-pi/pi-coding-agent": "*",
    "@types/bun": "^1.4.0",
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
